---
- name: "copy backend distribution files"
  copy:
    src: ~/project/backend/
    dest: ~/app

- name: "Install and Build"
  shell: |
    ls ~/app
    cd ~/app
    npm i
    npm run build

- name: "start pm2"
  shell: |
    pm2 start ~/app/dist/main.js
    pm2 ls