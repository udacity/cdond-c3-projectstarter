version: 2.1

jobs:
  frontend:
    docker:
      - image: node:12.18.1
    steps:
      - checkout
      - run:
          name: Install dependencies
          command: |
            cd frontend
            npm install
            npm run build
workflows:
  default:
    jobs:
      - frontend
