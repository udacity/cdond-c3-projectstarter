version: 2.1

jobs:
  deploy:
    docker:
      - image: amazon/aws-cli
    steps:
      - checkout
      - run:
          name: Infrastructure
          command: |
            ls
            aws cloudformation deploy \
              --template-file backend.yml \
              --stack-name udapeople-stack
workflows:
  default:
    jobs:
      - deploy
